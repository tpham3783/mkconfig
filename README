
# mkconfig is an application that converts kconfig file (.config) to
# a header file 

# Compile app:

  make


# Use app:
 
  ./mkconfig ../kconfig-frontends/example/


# Please review example/README for instructions how to utilize mkconfig with kconfig
